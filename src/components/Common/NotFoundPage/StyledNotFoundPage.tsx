import { Container, styled } from "@mui/material";

const StyledNotFoundPageContainer = styled(Container)(({ theme }) => ({
  height: "100%",
  display:'flex',
  flexDirection: "column",
  justifyContent: "center",
}));

export { StyledNotFoundPageContainer };
