import { styled, AppBar } from "@mui/material";

const StyledFooter = styled(AppBar)`
  text-align: center;
  && {
    top: auto;
    bottom: 0;
    background-color: #fff;
    color: #000;
  }
`;

export { StyledFooter };
